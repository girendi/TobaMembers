<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_member"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_member"
        android:supportsRtl="true"
        android:theme="@style/Theme.TobaMembers"
        tools:targetApi="31" >
        <activity
            android:name="com.girendi.tobamembers.persentation.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
        <activity android:name="com.girendi.tobamembers.persentation.login.LoginActivity"/>
        <activity android:name="com.girendi.tobamembers.persentation.register.RegisterActivity"/>
        <activity android:name="com.girendi.tobamembers.persentation.main.MainActivity" />
        <activity android:name="com.girendi.tobamembers.persentation.admin.AdminActivity" />
    </application>

</manifest>